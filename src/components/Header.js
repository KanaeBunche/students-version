import React from 'react';
import { Link } from 'react-router-dom';

function Header() {
  return (
    <header>
      <h1>Car Showcase</h1>
      <nav>
        {/* Step 1: Create a Link component for the home page that navigates to the root path ("/"). */}
        
        {/* Step 2: Create a Link component for the about page that navigates to "/about". */}
      </nav>
    </header>
  );
}

export default Header;
