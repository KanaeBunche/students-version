import React, { useState } from 'react';

function Counter() {
  // Step 1: Initialize a state variable to keep track of the count (count).

  return (
    <div>
      <button 
        // Step 2: Add an onClick handler to update the count by incrementing it by 1.
      >
        {/* Step 3: Display the current count in the button label. */}
      </button>
    </div>
  );
}

export default Counter;

